import React from 'react'
import Base from "../core/Base"
const UserDashboard = () => {
    return (
        <Base>
            <h1 title = "User Dashboard">
                This is User Dashboard
            </h1>
        </Base>
    )
}

export default UserDashboard